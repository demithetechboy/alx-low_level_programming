#include "main.h"
#include <stdio.h>

/**
 * reset_to_98 - code block
 * @n: parameter
 * Return: 0
 */

void reset_to_98(int *n)
{
n = 98;

printf("n=%d\n", n);
*n = 402;
printf("n=%d\n", n);
return (0);
}
